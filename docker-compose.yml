version: "3.7"
services: 
  geo-cms-db:
    image: kartoza/postgis:12.4
    restart: unless-stopped
    hostname: geo-cms-db
    container_name: geo-cms-db
    environment: 
      POSTGRES_USER: "docker"
      POSTGRES_PASS: "docker"
    ports: 
      - "5432:5432"
